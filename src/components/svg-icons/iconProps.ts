/** Shared by every hand-rolled SVG icon, mirroring the Tabler icon props. */
export interface IconProps {
    /** Pass "currentColor" so the icon inherits the button's active state. */
    color: string
    /** Square, in pixels. */
    size: number
}
